version: 2
updates:
  # Bun deps — update manifests AND bun.lock in all three packages.
  # Each directory is GROUPED into a single PR (patterns: ["*"]) so a refresh is
  # ~one PR per ecosystem, not one per dependency (avoids the first-run flood).
  - package-ecosystem: "bun"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 2
    groups:
      root-deps:
        patterns: ["*"]
  - package-ecosystem: "bun"
    directory: "/worker"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 2
    groups:
      worker-deps:
        patterns: ["*"]
  - package-ecosystem: "bun"
    directory: "/app"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 2
    groups:
      app-deps:
        patterns: ["*"]
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 2
    groups:
      actions:
        patterns: ["*"]
